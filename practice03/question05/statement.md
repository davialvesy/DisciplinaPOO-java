## Question 05

Write a Java program to create an Animal interface with a method called talk that takes no arguments and returns void. Create a Dog class that implements the Animal interface and overrides the talk() method to print "The dog is barking".